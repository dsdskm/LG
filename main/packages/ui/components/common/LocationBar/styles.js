import styled from 'styled-components'

// 페이지 상단(Title 아래)에 놓이는 선택 줄이다 — OrganizationSelector 와 같은 간격을 쓴다.
export const StyledLocationBar = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: flex-end;
  column-gap: 1.2rem;
  row-gap: 1.2rem;
  margin-bottom: 2rem;
`
